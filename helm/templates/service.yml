apiVersion: v1
kind: Service
metadata:
  name: {{ include "your-react-app.fullname" . }}
  labels:
    {{- include "your-react-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "your-react-app.selectorLabels" . | nindent 8 }}